apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-deployment
  labels:
    app: {{ .Chart.Name }}-deployment
spec:
  clusterIP: None
  selector:
    app: {{ .Chart.Name }}-deployment
  ports:
  - name: http
    port: 80
    protocol: TCP
  - name: ssh
    port: 2222
    protocol: TCP
    targetPort: 22
